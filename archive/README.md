# Archive

Historical research scripts and validation snapshots live here so the project root stays focused on active scraper code, docs, tools, and tests.

| Path | Contents |
|---|---|
| `archive/chp_api_explore/` | Older CHP API exploration scripts retained for reference only. |
| `archive/validation/` | Timestamped validation snapshots and reports that are not part of the active validation workflow. |

Active API documentation should stay in `documentation/`. Active reusable scripts should stay in `tools/`.
