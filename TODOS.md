- [x] Does all the update stores features of all the scrapers are updated to the changes I've done?
- [x] I want you to add to yourself some skill markdown for the agent to manage commits and push then on every time you finish a task.
- [x] `11:37:57 [INFO    ] ramilevi: store=8 — no products` - investigate this store in rami levy... why it has no products? what is it's location's name. change it from the default store id in the --help example of the main program
- [x] `11:41:50 [WARNING ] ramilevi: Could not parse net_content value '4*140': could not convert string to float: '4*140'` - find out to handle this case... why when I add log-level DEBUG argument it doesn't really add me more information and context to what happened... where the erroring code....
- [x] You need to redo the playwright api reserach on keshet teaamim I think you've missed that the families are important for the products feching because you're missing alot of products... like regural "ביצים" and "חזה עוף טרי ארוז" for example...
- [x] I actually don't see the keshet scrape function used... what is going on??
- [x] I feel like some scrapers when told to fetch the entire products of a branch/all branches they are limited by a cap... I don't like it. I want all of them to know how to fetch "every" but every!!! product!
- [ ] Looking at the shufersal results json I think that the deals aren't processed nicely. there is the minimum qty for the deal and the price is for the entire amount of qty. so there is a calculation we cna do to divide by the that... when there is actually a deal... but why foes the discount_percent not calcualated in that case?
- [ ] Remember to tick off todos you've finished in the file but also sync with the file (read new todos and tick off finished tasks)
- [ ] I have a shurfersal output in the output_dir directory which has 
- [ ] I want to update the api research documentation of each scraper to be more up to date with the code and the new analysis you've made for some scrapers. the api research should let the user understand how to manually do some instrumentation of the APIs. for example if there will be changes in the future... that he could do F12 in the browser and find if something changes. I also want you to write a markdown in the documentation that will be called AGENT_TARGET_DOCUEMTNATION.md and explain there that its a skill that will tell an agent how to explore all of the supermarket sites we have scrapers to. and how to explore them the right way with playwright and python aiohttp or curl requests... It should contain all of the supermarket sites we're scraping!!
- [ ] I want to build web application that will be a searcher and planner of shopping through the following scrapers I've implemented except CHP, I think the server should lazily load on each interval the entire database of the supermarkets (configurable) into a big DB. then it will process it based on user requests... for example the user can do a fuzzy search for "ביצים" and see the prices of them through all the supermarkets from the cheapest to the most expensive. Another section at the site will be that the user can add products (based on the DB) to a list and then compare the entire shopping list through all the supermarkets, sorted from cheapest to expensive. (Some stores might not have the specific product. which will lead to a warning that the comparison cannot be legit and they should remove that product...)